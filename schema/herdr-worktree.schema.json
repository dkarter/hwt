{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/dkarter/hwt/main/schema/herdr-worktree.schema.json",
  "title": "Herdr Worktree Configuration",
  "description": "Global and repository-specific settings used by hwt.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "agent": {
      "type": "string",
      "description": "Command an agent orchestrator should start in the new worktree.",
      "examples": ["opencode --port"]
    },
    "ticket_commands": {
      "type": "object",
      "description": "Optional named commands used by hwt create --ticket. Global and repository entries merge by name.",
      "propertyNames": { "pattern": "^[A-Za-z][A-Za-z0-9_-]*$" },
      "additionalProperties": {
        "type": "object",
        "additionalProperties": false,
        "required": ["command"],
        "properties": {
          "command": {
            "type": "array",
            "minItems": 1,
            "items": { "type": "string", "minLength": 1 },
            "description": "Direct argv. The optional ticket input replaces {input}; an exact {input} argument is omitted when no input is provided."
          },
          "output": {
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "branch": {
                "type": "string",
                "pattern": "^[A-Za-z_][A-Za-z0-9_-]*(\\.[A-Za-z_][A-Za-z0-9_-]*)*$",
                "default": "branchName",
                "description": "Dot-separated selector for the string branch field in command JSON output."
              },
              "metadata": {
                "type": "object",
                "propertyNames": {
                  "pattern": "^[A-Za-z_][A-Za-z0-9_-]*(\\.[A-Za-z_][A-Za-z0-9_-]*)*$"
                },
                "additionalProperties": {
                  "type": "string",
                  "pattern": "^[A-Za-z_][A-Za-z0-9_-]*(\\.[A-Za-z_][A-Za-z0-9_-]*)*$"
                },
                "description": "Persisted metadata names mapped to dot-separated selectors for string fields in command JSON output."
              }
            }
          }
        }
      },
      "examples": [
        {
          "create": {
            "command": ["lnr", "quick", "{input}", "--json"],
            "output": {
              "branch": "branchName",
              "metadata": { "identifier": "issueId", "title": "title", "url": "url" }
            }
          }
        }
      ]
    },
    "review_command": {
      "type": "array",
      "description": "Argument vector launched in a review workspace. hwt shell-quotes every configured argument before asking Herdr to run it and never appends pull request metadata.",
      "default": ["tuicr"],
      "minItems": 1,
      "items": { "type": "string", "minLength": 1 },
      "examples": [["tuicr"], ["custom-review", "--local"]]
    },
    "worktree_dir": {
      "type": "string",
      "description": "Directory for worktrees. Relative paths resolve from the repository root.",
      "examples": ["../", "~/.herdr/worktrees"]
    },
    "worktree_naming": {
      "type": "string",
      "description": "Strategy used to derive the worktree directory name from its branch.",
      "default": "full",
      "enum": ["full", "basename"]
    },
    "worktree_prefix": {
      "type": "string",
      "description": "Prefix added to generated worktree directory names.",
      "examples": ["houston-"]
    },
    "urls": {
      "type": "object",
      "description": "Named URLs exposed through hwt url. Values may be template strings, template objects, or service objects that return a configured port service's generated URL verbatim. Objects may have an optional display label. The default pr entry targets GitHub and can be overridden. Built-in template placeholders are {repository}, {branch}, {sanitized_branch}, {worktree}, {hostname}, {pr_host}, {pr_owner}, {pr_repository}, and {pr_number}; custom metadata placeholders may be dot-separated.",
      "propertyNames": { "pattern": "^[A-Za-z][A-Za-z0-9_-]*$" },
      "additionalProperties": {
        "oneOf": [
          {
            "type": "string",
            "minLength": 1,
            "format": "uri-template"
          },
          {
            "type": "object",
            "additionalProperties": false,
            "required": ["template"],
            "properties": {
              "template": { "type": "string", "minLength": 1, "format": "uri-template" },
              "label": { "type": "string", "minLength": 1 }
            }
          },
          {
            "type": "object",
            "additionalProperties": false,
            "required": ["service"],
            "properties": {
              "service": {
                "type": "string",
                "pattern": "^[A-Za-z][A-Za-z0-9_-]*$",
                "description": "Configured ports.services entry whose generated current-worktree URL is returned verbatim."
              },
              "label": { "type": "string", "minLength": 1 }
            }
          }
        ]
      },
      "examples": [
        {
          "local": { "service": "web", "label": "Local app" },
          "preview": {
            "template": "https://{sanitized_branch}.preview.example.com",
            "label": "Branch preview"
          }
        }
      ]
    },
    "metadata": {
      "type": "object",
      "description": "Static and command-backed custom metadata used by named URL templates.",
      "additionalProperties": false,
      "properties": {
        "values": {
          "type": "object",
          "description": "Static string values. Dot-separated names are supported.",
          "propertyNames": { "pattern": "^[A-Za-z_][A-Za-z0-9_-]*(\\.[A-Za-z_][A-Za-z0-9_-]*)*$" },
          "additionalProperties": { "type": "string" }
        },
        "commands": {
          "type": "object",
          "description": "Named argv commands run lazily without a shell. Each command returns one JSON object with string values, exposed below the command name.",
          "propertyNames": { "pattern": "^[A-Za-z][A-Za-z0-9_-]*$" },
          "additionalProperties": {
            "type": "array",
            "minItems": 1,
            "items": { "type": "string", "minLength": 1 }
          }
        }
      }
    },
    "files": {
      "type": "object",
      "description": "File operations performed after creating a worktree.",
      "additionalProperties": false,
      "properties": {
        "copy": {
          "type": "array",
          "description": "Repository-relative files or directories copied into new worktrees. String entries inherit the files defaults; object entries may override them. Missing sources are ignored. Use <global> in a project config to include global entries.",
          "items": {
            "oneOf": [
              { "type": "string" },
              {
                "type": "object",
                "additionalProperties": false,
                "required": ["path"],
                "allOf": [
                  {
                    "not": {
                      "required": ["copy_on_write", "symlink"],
                      "properties": {
                        "copy_on_write": { "const": true },
                        "symlink": { "const": true }
                      }
                    }
                  }
                ],
                "properties": {
                  "path": {
                    "type": "string",
                    "description": "Repository-relative source and destination path."
                  },
                  "parallel": {
                    "type": "boolean",
                    "description": "Override whether this entry participates in parallel copying. A false value makes this entry a scheduling barrier."
                  },
                  "copy_on_write": {
                    "type": "boolean",
                    "description": "Experimentally attempt a filesystem copy-on-write clone before falling back to a normal copy."
                  },
                  "symlink": {
                    "type": "boolean",
                    "description": "Symlink this path to the source checkout instead of copying it. Cannot be combined with copy_on_write."
                  }
                }
              }
            ]
          }
        },
        "parallel": {
          "type": "boolean",
          "description": "Copy configured entries in parallel. Set to false to copy them sequentially.",
          "default": true
        },
        "copy_on_write": {
          "type": "boolean",
          "description": "Experimentally attempt filesystem copy-on-write clones before falling back to normal copies.",
          "default": false
        }
      }
    },
    "post_create": {
      "type": "array",
      "description": "Shell commands run in order from the new worktree root. Use <global> in a project config to include global commands.",
      "items": { "type": "string", "minLength": 1 }
    },
    "ports": {
      "type": "object",
      "description": "Stable per-worktree ports reserved in a machine-local registry.",
      "additionalProperties": false,
      "properties": {
        "start": {
          "type": "integer",
          "minimum": 1024,
          "maximum": 65535,
          "default": 20000
        },
        "end": {
          "type": "integer",
          "minimum": 1024,
          "maximum": 65535,
          "default": 39999
        },
        "services": {
          "type": "array",
          "description": "Service names exposed as HWT_PORT_<NAME> and HWT_URL_<NAME> variables. Direct URLs default to RFC 6761 localhost subdomains unless managed local DNS is enabled.",
          "uniqueItems": true,
          "items": {
            "type": "string",
            "pattern": "^(<global>|[A-Za-z][A-Za-z0-9_-]*)$"
          }
        },
        "url_template": {
          "type": "string",
          "default": "http://{worktree}.{service}.localhost:{port}",
          "description": "Direct service URL template used when managed local DNS is disabled. Supports {worktree}, {service}, {hostname}, and {port}."
        }
      }
    },
    "local_dns": {
      "type": "object",
      "description": "Opt-in HWT-owned dnsmasq and Caddy snippet generation. Disabled services use zero-setup RFC 6761 localhost URLs.",
      "additionalProperties": false,
      "properties": {
        "enabled": {
          "type": "boolean",
          "default": false,
          "description": "Register routes during create, copy, env, and remove lifecycle operations."
        },
        "domain": {
          "type": "string",
          "default": "hwt.test",
          "minLength": 1,
          "maxLength": 189,
          "description": "Local suffix for generated hostnames. DNS syntax is checked by hwt."
        },
        "reload": {
          "type": "array",
          "description": "Optional argv command run after owned snippets change. Supports {caddyfile}, {dnsmasq}, and {state_dir}; no shell is used.",
          "items": { "type": "string", "minLength": 1 }
        }
      }
    },
    "environment": {
      "type": "object",
      "description": "Non-secret variables written to .env.worktree and supplied to post_create commands.",
      "additionalProperties": false,
      "properties": {
        "variables": {
          "type": "object",
          "additionalProperties": { "type": "string" },
          "propertyNames": {
            "pattern": "^(?!HWT_)[A-Za-z_][A-Za-z0-9_]*$"
          }
        }
      }
    }
  }
}
