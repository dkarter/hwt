# Notes for building the website

- ALWAYS use aube aubx etc instead of npm
- Test your work using playwright mcp whenever making UI changes
- If the mcp complains that playwright is not installed - install it with the mcp tool that it provides
- Verify changes work on both dark and light mode. dark mode is the default.
- Files should be formatted using dprint or the mise task -> `mise fmt`
